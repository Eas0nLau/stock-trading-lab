# Windows 环境安装

## 1. 安装基础软件

- Python 由 `uv` 管理，项目要求 Python 3.12。
- 安装 [uv](https://docs.astral.sh/uv/getting-started/installation/)。
- 安装 Node.js 24 和 npm，可使用 [nvm-windows](https://github.com/coreybutler/nvm-windows/releases)。
- 安装 MySQL 8 和 Redis，或使用 `init/docker/` 下的 Docker Compose 配置。

## 2. 配置项目

在项目根目录创建 `.env`，参考 `.env.example`：

```dotenv
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=change-me
MYSQL_DATABASE=stock_trading_lab
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_DATABASE=0
```

Tushare、DeepSeek 和通达信配置为可选项，使用对应功能时再填写。

## 3. 安装依赖

```powershell
uv sync --all-groups --frozen
npm --prefix front install
```

新环境使用 `init/stock_trading_lab_v2.sql` 创建英文数据库结构。旧数据库升级按 `docs/database-migrations.md` 执行，不要直接导入初始化 SQL 覆盖存量库。

## 4. 启动

```powershell
.\启动项目.ps1
```

也可以手动启动：

```powershell
uv run --frozen python app.py
```

后端端口为 `8051`，前端端口为 `8990`。

## 5. 验证

```powershell
uv run pytest -q
uv run python -m compileall -q src app.py config.py front_run.py
npm --prefix front run build
```
